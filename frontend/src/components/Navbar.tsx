import { Container, Flex, Text } from '@chakra-ui/react';

const Navbar = () => {
    return <Container>
        <Flex>
            <Text>Test</Text>
        </Flex>
    </Container>;
}

export default Navbar;